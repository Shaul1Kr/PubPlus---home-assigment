services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: mysecretpassword
      POSTGRES_USER: postgres
      POSTGRES_DB: PubPlus
    ports:
      - 5431:5432
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
